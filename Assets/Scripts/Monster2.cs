using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Monster2 : MonsterScript
{
    // Start is called before the first frame update
    void Start()
    {
        setUpMonster(1);

    }

    // Update is called once per frame
    void Update()
    {
        exist();

    }
}
